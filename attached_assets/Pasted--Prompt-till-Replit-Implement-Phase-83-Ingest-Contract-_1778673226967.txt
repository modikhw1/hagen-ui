 Prompt till Replit:

  Implement Phase 83 — Ingest Contract Health Surface.

  Repository boundary:
  Do not change dependency/runtime/packaging configuration.
  Do not edit package.json, pnpm-lock.yaml, pnpm-workspace.yaml, Dockerfile, .npmrc, Railway/Replit/Vite/React
  dependency config.
  Do not create or commit attached_assets/Pasted*.txt or pasted prompt artifacts.
  Treat current main as Railway deploy ground truth.

  Goal:
  Add a read-only health surface for the ingest/concept contract so operators can see whether the library is aligned
  with the canonical ingest contract before any backfill.

  Context:
  Phase 82 added ingest_run traceability. Supabase currently has 33 concepts, 0 with overrides_version, 22 with
  scene_breakdown. Existing ingest_runs are old and do not yet have trace fields.

  Required work:
  1. Add API endpoint, preferably:
     GET /api/studio/ingest-contract-health
     CM/admin protected.

  2. Endpoint should return:
     - total concepts
     - concepts with overrides_version
     - concepts missing overrides_version
     - concepts with deprecated estimatedBudget
     - concepts with deprecated trendLevel
     - concepts where hasScript exists while script_mode exists
     - concepts with scene_breakdown
     - recent ingest_runs with status, stage, concept_id, customer_profile_id, hagen_request_id, hagen_contract_version,
  warnings, created_at, updated_at

  3. Add a compact read-only panel in /studio/concepts:
     - show contract health counts
     - show warning if concepts are missing overrides_version
     - show recent ingest runs
     - no mutation buttons yet
     - no DB migrations

  4. Keep UI consistent with existing /studio/concepts design language.

  5. Add docs/agent-plans/83-ingest-contract-health.md:
     - files changed
     - endpoint response shape
     - what the panel shows
     - what remains for Phase 84, likely dry-run/backfill normalization

  Verification:
  - Run api-server typecheck and tests.
  - Run frontend build if frontend changed.
  - Do not touch /admin/demos.