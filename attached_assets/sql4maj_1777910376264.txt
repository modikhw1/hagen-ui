-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.admin_billing_reconcile_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  requested_by uuid,
  scope text NOT NULL CHECK (scope = ANY (ARRAY['invoices'::text, 'subscriptions'::text, 'all'::text])),
  environment text NOT NULL CHECK (environment = ANY (ARRAY['live'::text, 'test'::text])),
  since timestamp with time zone,
  status text NOT NULL DEFAULT 'queued'::text CHECK (status = ANY (ARRAY['queued'::text, 'running'::text, 'completed'::text, 'failed'::text])),
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  result jsonb,
  error_message text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  started_at timestamp with time zone,
  finished_at timestamp with time zone,
  CONSTRAINT admin_billing_reconcile_jobs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.admin_customer_action_locks (
  lock_key text NOT NULL,
  customer_profile_id uuid NOT NULL,
  request_id text NOT NULL,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL,
  CONSTRAINT admin_customer_action_locks_pkey PRIMARY KEY (lock_key),
  CONSTRAINT admin_customer_action_locks_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.admin_customer_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid NOT NULL,
  author_user_id uuid,
  author_name text,
  body text NOT NULL CHECK (length(TRIM(BOTH FROM body)) > 0 AND length(body) <= 4000),
  pinned boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT admin_customer_notes_pkey PRIMARY KEY (id),
  CONSTRAINT admin_customer_notes_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT admin_customer_notes_author_user_id_fkey FOREIGN KEY (author_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.admin_idempotency_keys (
  key text NOT NULL,
  operation text NOT NULL,
  customer_id uuid,
  demo_id uuid,
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT admin_idempotency_keys_pkey PRIMARY KEY (key)
);
CREATE TABLE public.admin_notification_seen (
  admin_id uuid NOT NULL,
  surface text NOT NULL CHECK (surface = ANY (ARRAY['overview'::text, 'notifications'::text])),
  last_seen_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT admin_notification_seen_pkey PRIMARY KEY (admin_id, surface)
);
CREATE TABLE public.admin_request_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  request_id text NOT NULL,
  actor_user_id uuid,
  route text NOT NULL,
  action text,
  entity_id text,
  status_code integer NOT NULL,
  duration_ms integer NOT NULL,
  CONSTRAINT admin_request_log_pkey PRIMARY KEY (id)
);
CREATE TABLE public.admin_user_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  role USER-DEFINED NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT admin_user_roles_pkey PRIMARY KEY (id),
  CONSTRAINT admin_user_roles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.attention_snoozes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  subject_type USER-DEFINED NOT NULL,
  subject_id text NOT NULL,
  snoozed_until timestamp with time zone,
  note text,
  snoozed_by_admin_id uuid NOT NULL,
  snoozed_at timestamp with time zone NOT NULL DEFAULT now(),
  released_at timestamp with time zone,
  release_reason text CHECK ((release_reason = ANY (ARRAY['expired'::text, 'escalated'::text, 'manual'::text])) OR release_reason IS NULL),
  CONSTRAINT attention_snoozes_pkey PRIMARY KEY (id),
  CONSTRAINT attention_snoozes_snoozed_by_admin_id_fkey FOREIGN KEY (snoozed_by_admin_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.audit_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  actor_user_id uuid,
  actor_email text,
  actor_role text,
  action text NOT NULL,
  entity_type text NOT NULL,
  entity_id text,
  before_state jsonb,
  after_state jsonb,
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_log_pkey PRIMARY KEY (id),
  CONSTRAINT audit_log_actor_user_id_fkey FOREIGN KEY (actor_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.auto_edit_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  template_id uuid NOT NULL,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'ready'::text, 'rendering'::text, 'rendered'::text, 'failed'::text])),
  template_snapshot jsonb NOT NULL DEFAULT '{}'::jsonb,
  slot_fills jsonb NOT NULL DEFAULT '[]'::jsonb,
  text_overlays jsonb NOT NULL DEFAULT '[]'::jsonb,
  audio_selection jsonb,
  render_progress numeric,
  render_url text,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT auto_edit_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT auto_edit_sessions_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT auto_edit_sessions_template_id_fkey FOREIGN KEY (template_id) REFERENCES public.auto_edit_templates(id),
  CONSTRAINT auto_edit_sessions_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.auto_edit_templates (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid,
  concept_id text,
  name text NOT NULL,
  aspect_ratio text NOT NULL DEFAULT '9:16'::text CHECK (aspect_ratio = ANY (ARRAY['9:16'::text, '16:9'::text, '1:1'::text])),
  total_duration numeric NOT NULL DEFAULT 0,
  replicability_score numeric NOT NULL DEFAULT 0,
  description text NOT NULL DEFAULT ''::text,
  required_elements jsonb NOT NULL DEFAULT '[]'::jsonb,
  variable_elements jsonb NOT NULL DEFAULT '[]'::jsonb,
  difficulty text NOT NULL DEFAULT 'medium'::text CHECK (difficulty = ANY (ARRAY['low'::text, 'medium'::text, 'high'::text])),
  content_style text NOT NULL DEFAULT ''::text,
  content_format text NOT NULL DEFAULT ''::text,
  target_audience text,
  pacing_score numeric NOT NULL DEFAULT 0,
  tags jsonb NOT NULL DEFAULT '[]'::jsonb,
  slots jsonb NOT NULL DEFAULT '[]'::jsonb,
  text_overlays jsonb NOT NULL DEFAULT '[]'::jsonb,
  audio jsonb NOT NULL DEFAULT '{}'::jsonb,
  design_system_id uuid,
  color_mood jsonb,
  typography_hint jsonb,
  preview_url text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT auto_edit_templates_pkey PRIMARY KEY (id),
  CONSTRAINT auto_edit_templates_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT auto_edit_templates_design_system_id_fkey FOREIGN KEY (design_system_id) REFERENCES public.customer_design_systems(id),
  CONSTRAINT auto_edit_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.cm_absences (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cm_id uuid NOT NULL,
  customer_profile_id uuid,
  backup_cm_id uuid,
  absence_type text NOT NULL DEFAULT 'vacation'::text CHECK (absence_type = ANY (ARRAY['vacation'::text, 'sick'::text, 'parental_leave'::text, 'training'::text, 'temporary_coverage'::text, 'other'::text])),
  compensation_mode text NOT NULL DEFAULT 'covering_cm'::text CHECK (compensation_mode = ANY (ARRAY['covering_cm'::text, 'primary_cm'::text])),
  starts_on date NOT NULL,
  ends_on date NOT NULL,
  note text,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT cm_absences_pkey PRIMARY KEY (id),
  CONSTRAINT cm_absences_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.team_members(id),
  CONSTRAINT cm_absences_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT cm_absences_backup_cm_id_fkey FOREIGN KEY (backup_cm_id) REFERENCES public.team_members(id),
  CONSTRAINT cm_absences_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.cm_activities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cm_user_id uuid,
  cm_email text NOT NULL,
  customer_profile_id uuid,
  activity_type text NOT NULL CHECK (activity_type = ANY (ARRAY['concept_added'::text, 'concept_removed'::text, 'concept_customized'::text, 'concept_created'::text, 'concept_updated'::text, 'concept_sent'::text, 'email_sent'::text, 'gameplan_updated'::text, 'customer_created'::text, 'customer_updated'::text, 'customer_invited'::text, 'upload'::text])),
  description text NOT NULL,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  cm_id uuid,
  cm_name text,
  type text,
  CONSTRAINT cm_activities_pkey PRIMARY KEY (id),
  CONSTRAINT cm_activities_cm_user_id_fkey FOREIGN KEY (cm_user_id) REFERENCES public.profiles(id),
  CONSTRAINT cm_activities_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT cm_activities_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.cm_assignments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  cm_id uuid,
  valid_from date NOT NULL DEFAULT CURRENT_DATE,
  valid_to date,
  scheduled_change jsonb,
  handover_note text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT cm_assignments_pkey PRIMARY KEY (id),
  CONSTRAINT cm_assignments_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT cm_assignments_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.cm_interactions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cm_id uuid NOT NULL,
  customer_id uuid,
  type text NOT NULL CHECK (type = ANY (ARRAY['login'::text, 'feedplan_edit'::text, 'concept_added'::text, 'email_sent'::text, 'note_added'::text, 'tiktok_upload_synced'::text])),
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT cm_interactions_pkey PRIMARY KEY (id),
  CONSTRAINT cm_interactions_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.team_members(id),
  CONSTRAINT cm_interactions_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.cm_library_visits (
  cm_id uuid NOT NULL,
  last_visit timestamp with time zone DEFAULT now(),
  CONSTRAINT cm_library_visits_pkey PRIMARY KEY (cm_id),
  CONSTRAINT cm_library_visits_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.cm_notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  from_cm_id uuid NOT NULL,
  customer_id uuid,
  message text NOT NULL,
  priority USER-DEFINED NOT NULL DEFAULT 'normal'::cm_notification_priority,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  resolved_at timestamp with time zone,
  resolved_by_admin_id uuid,
  resolution_note text,
  CONSTRAINT cm_notifications_pkey PRIMARY KEY (id),
  CONSTRAINT cm_notifications_from_cm_id_fkey FOREIGN KEY (from_cm_id) REFERENCES public.team_members(id),
  CONSTRAINT cm_notifications_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT cm_notifications_resolved_by_admin_id_fkey FOREIGN KEY (resolved_by_admin_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.cm_tags (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cm_id uuid NOT NULL,
  name text NOT NULL,
  color text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cm_tags_pkey PRIMARY KEY (id),
  CONSTRAINT cm_tags_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.collections (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  cm_id uuid NOT NULL,
  name text NOT NULL,
  color text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT collections_pkey PRIMARY KEY (id),
  CONSTRAINT collections_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.concept_field_regenerations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  concept_id text NOT NULL,
  field text NOT NULL CHECK (field = ANY (ARRAY['headline_sv'::text, 'description_sv'::text, 'whyItWorks_sv'::text, 'script_sv'::text])),
  model text NOT NULL,
  prompt_version text NOT NULL DEFAULT 'v1'::text,
  output text NOT NULL,
  output_chars integer DEFAULT length(output),
  was_picked boolean NOT NULL DEFAULT false,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT concept_field_regenerations_pkey PRIMARY KEY (id),
  CONSTRAINT concept_field_regenerations_concept_id_fkey FOREIGN KEY (concept_id) REFERENCES public.concepts(id),
  CONSTRAINT concept_field_regenerations_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.concept_ownership_audit (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  concept_id text NOT NULL,
  previous_owner uuid,
  new_owner uuid NOT NULL,
  actor uuid NOT NULL,
  reason text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT concept_ownership_audit_pkey PRIMARY KEY (id),
  CONSTRAINT concept_ownership_audit_concept_id_fkey FOREIGN KEY (concept_id) REFERENCES public.concepts(id)
);
CREATE TABLE public.concept_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  concept_id text NOT NULL,
  version integer NOT NULL,
  backend_data jsonb NOT NULL,
  overrides jsonb NOT NULL,
  changed_by uuid,
  change_summary text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT concept_versions_pkey PRIMARY KEY (id),
  CONSTRAINT concept_versions_concept_id_fkey FOREIGN KEY (concept_id) REFERENCES public.concepts(id),
  CONSTRAINT concept_versions_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.concepts (
  id text NOT NULL,
  source text NOT NULL CHECK (source = ANY (ARRAY['hagen'::text, 'cm_created'::text])),
  created_by uuid,
  backend_data jsonb NOT NULL,
  overrides jsonb DEFAULT '{}'::jsonb,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  version integer DEFAULT 1,
  previous_version jsonb,
  reviewed_at timestamp with time zone,
  reviewed_by uuid,
  CONSTRAINT concepts_pkey PRIMARY KEY (id),
  CONSTRAINT concepts_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT concepts_reviewed_by_fkey FOREIGN KEY (reviewed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.credit_note_operations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  operation_type text NOT NULL CHECK (operation_type = ANY (ARRAY['credit_note_only'::text, 'credit_note_and_reissue'::text, 'refund'::text])),
  customer_profile_id uuid NOT NULL,
  source_invoice_id text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'credit_note_created'::text, 'reissue_created'::text, 'completed'::text, 'failed'::text])),
  requires_attention boolean NOT NULL DEFAULT false,
  attention_reason text,
  stripe_credit_note_id text,
  stripe_reissue_invoice_id text,
  stripe_refund_id text,
  amount_ore bigint,
  idempotency_key text NOT NULL UNIQUE,
  error_message text,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT credit_note_operations_pkey PRIMARY KEY (id),
  CONSTRAINT credit_note_operations_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.cron_run_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  finished_at timestamp with time zone,
  processed integer NOT NULL DEFAULT 0,
  imported integer NOT NULL DEFAULT 0,
  stats_updated integer NOT NULL DEFAULT 0,
  calls_used integer NOT NULL DEFAULT 0,
  budget_remaining integer NOT NULL DEFAULT 0,
  budget_exceeded boolean NOT NULL DEFAULT false,
  stale_locks_cleared integer NOT NULL DEFAULT 0,
  errors jsonb,
  CONSTRAINT cron_run_log_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customer_concepts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid NOT NULL,
  concept_id text,
  custom_headline text,
  custom_description text,
  custom_why_it_works text,
  custom_instructions text,
  custom_target_audience text,
  custom_script text,
  custom_production_notes ARRAY,
  match_percentage integer DEFAULT 85 CHECK (match_percentage >= 0 AND match_percentage <= 100),
  status text DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'sent'::text, 'produced'::text, 'archived'::text, 'history_import'::text])),
  notes text,
  added_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  base_concept_version integer DEFAULT 1,
  customer_id uuid NOT NULL,
  cm_id uuid,
  why_it_fits text,
  filming_instructions text,
  tiktok_url text,
  feed_slot integer CHECK (feed_slot >= 1 AND feed_slot <= 9),
  sent_at timestamp with time zone,
  produced_at timestamp with time zone,
  feed_order integer,
  tags ARRAY DEFAULT '{}'::text[],
  collection_id uuid,
  cm_note text,
  content_overrides jsonb,
  tiktok_thumbnail_url text,
  tiktok_views integer,
  tiktok_likes integer,
  tiktok_comments integer,
  tiktok_watch_time_seconds integer,
  tiktok_last_synced_at timestamp with time zone,
  planned_publish_at timestamp with time zone,
  content_loaded_at timestamp with time zone,
  content_loaded_seen_at timestamp with time zone,
  published_at timestamp with time zone,
  reconciled_customer_concept_id uuid,
  reconciled_by_cm_id uuid,
  reconciled_at timestamp with time zone,
  partner_name text,
  profile_name text,
  profile_image_url text,
  visual_variant text DEFAULT 'default'::text,
  history_source text,
  observed_profile_handle text,
  provider_name text,
  provider_video_id text,
  first_observed_at timestamp with time zone,
  last_observed_at timestamp with time zone,
  collaborator_reach text,
  collaborator_avatar_url text,
  scope ARRAY NOT NULL DEFAULT '{}'::text[],
  price integer,
  confirmed boolean NOT NULL DEFAULT false,
  collaboration_note text,
  collaboration_date_type text,
  CONSTRAINT customer_concepts_pkey PRIMARY KEY (id),
  CONSTRAINT customer_concepts_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_concepts_concept_id_fkey FOREIGN KEY (concept_id) REFERENCES public.concepts(id),
  CONSTRAINT customer_concepts_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_concepts_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id),
  CONSTRAINT fk_customer_concepts_collection FOREIGN KEY (collection_id) REFERENCES public.collections(id),
  CONSTRAINT customer_concepts_reconciled_customer_concept_id_fkey FOREIGN KEY (reconciled_customer_concept_id) REFERENCES public.customer_concepts(id),
  CONSTRAINT customer_concepts_reconciled_by_cm_id_fkey FOREIGN KEY (reconciled_by_cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.customer_design_systems (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid,
  name text NOT NULL,
  definition jsonb NOT NULL,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_design_systems_pkey PRIMARY KEY (id),
  CONSTRAINT customer_design_systems_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.customer_discounts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  stripe_coupon_id text,
  stripe_promotion_code_id text,
  discount_type text NOT NULL CHECK (discount_type = ANY (ARRAY['percent'::text, 'amount'::text, 'free_months'::text])),
  value integer NOT NULL DEFAULT 0 CHECK (value >= 0),
  duration_months integer CHECK (duration_months IS NULL OR duration_months > 0),
  ongoing boolean NOT NULL DEFAULT false,
  start_date date,
  end_date date,
  active boolean NOT NULL DEFAULT true,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_discounts_pkey PRIMARY KEY (id),
  CONSTRAINT customer_discounts_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_discounts_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.customer_game_plans (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL UNIQUE,
  html text NOT NULL DEFAULT ''::text,
  plain_text text NOT NULL DEFAULT ''::text,
  editor_version integer NOT NULL DEFAULT 1,
  updated_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_game_plans_pkey PRIMARY KEY (id),
  CONSTRAINT customer_game_plans_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_game_plans_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.customer_invites (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  email text NOT NULL,
  token text NOT NULL DEFAULT encode(gen_random_bytes(32), 'hex'::text) UNIQUE,
  provider text NOT NULL DEFAULT 'supabase_invite'::text,
  invite_link text,
  expires_at timestamp with time zone,
  consumed_at timestamp with time zone,
  superseded_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_invites_pkey PRIMARY KEY (id),
  CONSTRAINT customer_invites_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_invites_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.customer_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  cm_id uuid NOT NULL,
  content text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  content_html text,
  note_type text NOT NULL DEFAULT 'update'::text,
  primary_customer_concept_id uuid,
  references jsonb NOT NULL DEFAULT '[]'::jsonb,
  attachments jsonb NOT NULL DEFAULT '[]'::jsonb,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_notes_pkey PRIMARY KEY (id),
  CONSTRAINT customer_notes_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_notes_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id),
  CONSTRAINT customer_notes_primary_customer_concept_id_fkey FOREIGN KEY (primary_customer_concept_id) REFERENCES public.customer_concepts(id)
);
CREATE TABLE public.customer_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  business_name text NOT NULL,
  contact_email text,
  monthly_price integer DEFAULT 0,
  price_start_date date,
  price_end_date date,
  contacts jsonb DEFAULT '[]'::jsonb,
  profile_data jsonb DEFAULT '{}'::jsonb,
  game_plan jsonb DEFAULT '{"goals": [], "title": "", "description": "", "contentThemes": [], "targetAudience": "", "postingFrequency": ""}'::jsonb,
  concepts jsonb DEFAULT '[]'::jsonb,
  status text DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'active'::text, 'archived'::text, 'invited'::text, 'agreed'::text, 'prospect'::text])),
  stripe_customer_id text,
  stripe_subscription_id text,
  invited_at timestamp with time zone,
  agreed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  invoice_text text,
  scope_items jsonb DEFAULT '[]'::jsonb,
  subscription_interval text DEFAULT 'month'::text,
  customer_contact_name text,
  account_manager text DEFAULT 'Mahmoud'::text,
  logo_url text,
  brief jsonb DEFAULT '{"tone": "", "constraints": "", "current_focus": ""}'::jsonb,
  contract_start_date date,
  billing_day_of_month smallint DEFAULT 25 CHECK (billing_day_of_month >= 1 AND billing_day_of_month <= 28),
  first_invoice_behavior text DEFAULT 'prorated'::text CHECK (first_invoice_behavior = ANY (ARRAY['prorated'::text, 'full'::text, 'free_until_anchor'::text])),
  pricing_status text DEFAULT 'fixed'::text CHECK (pricing_status = ANY (ARRAY['fixed'::text, 'unknown'::text])),
  discount_type text DEFAULT 'none'::text CHECK (discount_type IS NULL OR (discount_type = ANY (ARRAY['none'::text, 'percent'::text, 'amount'::text, 'free_months'::text]))),
  discount_value integer DEFAULT 0 CHECK (discount_value >= 0),
  discount_duration_months integer CHECK (discount_duration_months IS NULL OR discount_duration_months > 0),
  discount_start_date date,
  discount_end_date date,
  upcoming_monthly_price integer,
  upcoming_price_effective_date date,
  contract_metadata jsonb DEFAULT '{}'::jsonb,
  account_manager_profile_id uuid,
  tiktok_handle text,
  last_history_sync_at timestamp with time zone,
  tiktok_profile_url text,
  pending_history_advance_at timestamp with time zone,
  upload_schedule ARRAY DEFAULT '{}'::text[],
  last_upload_at timestamp with time zone,
  phone text,
  discount_ends_at timestamp with time zone,
  upcoming_price_change_at timestamp with time zone,
  upcoming_price_change_value numeric,
  next_invoice_date date,
  user_id uuid,
  tiktok_profile_pic_url text,
  tiktok_profile_synced_at timestamp with time zone,
  concepts_per_week smallint NOT NULL DEFAULT 2 CHECK (concepts_per_week >= 0 AND concepts_per_week <= 7),
  paused_until date,
  onboarding_state text NOT NULL DEFAULT 'invited'::text CHECK (onboarding_state = ANY (ARRAY['invited'::text, 'cm_ready'::text, 'live'::text, 'settled'::text])),
  onboarding_state_changed_at timestamp with time zone NOT NULL DEFAULT now(),
  from_demo_id uuid,
  operation_lock_until timestamp with time zone,
  tiktok_user_id text,
  expected_concepts_per_week smallint NOT NULL DEFAULT 2 CHECK (expected_concepts_per_week >= 0 AND expected_concepts_per_week <= 7),
  invite_attempt_nonce bigint NOT NULL DEFAULT 0,
  archived_at timestamp with time zone,
  lifecycle_state text NOT NULL DEFAULT 'draft'::text CHECK (lifecycle_state = ANY (ARRAY['draft'::text, 'invited'::text, 'active'::text, 'paused'::text, 'archived'::text])),
  lifecycle_state_changed_at timestamp with time zone NOT NULL DEFAULT now(),
  cm_avatar_url text,
  cm_initial_color text,
  latest_planned_publish_date timestamp with time zone,
  last_published_at timestamp with time zone,
  upcoming_price_change jsonb,
  last_sync_error text,
  CONSTRAINT customer_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT customer_profiles_account_manager_profile_id_fkey FOREIGN KEY (account_manager_profile_id) REFERENCES public.profiles(id),
  CONSTRAINT customer_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT customer_profiles_from_demo_id_fkey FOREIGN KEY (from_demo_id) REFERENCES public.demos(id)
);
CREATE TABLE public.customer_subscription_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  stripe_subscription_id text,
  stripe_schedule_id text,
  stripe_price_id text,
  mode text NOT NULL CHECK (mode = ANY (ARRAY['now'::text, 'next_period'::text])),
  previous_price_ore integer NOT NULL DEFAULT 0 CHECK (previous_price_ore >= 0),
  next_price_ore integer NOT NULL CHECK (next_price_ore >= 0),
  effective_date date NOT NULL,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_subscription_history_pkey PRIMARY KEY (id),
  CONSTRAINT customer_subscription_history_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_subscription_history_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.customer_upcoming_price_changes (
  customer_id uuid NOT NULL,
  stripe_subscription_id text,
  stripe_schedule_id text,
  price_ore integer NOT NULL CHECK (price_ore >= 0),
  effective_date date NOT NULL,
  created_by uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT customer_upcoming_price_changes_pkey PRIMARY KEY (customer_id),
  CONSTRAINT customer_upcoming_price_changes_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT customer_upcoming_price_changes_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.debug_log (
  id integer NOT NULL DEFAULT nextval('debug_log_id_seq'::regclass),
  ts timestamp with time zone DEFAULT now(),
  msg text,
  CONSTRAINT debug_log_pkey PRIMARY KEY (id)
);
CREATE TABLE public.demos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_name text NOT NULL,
  contact_name text,
  contact_email text,
  tiktok_handle text,
  tiktok_profile_pic_url text,
  proposed_concepts_per_week smallint CHECK (proposed_concepts_per_week >= 1 AND proposed_concepts_per_week <= 5),
  proposed_price_ore integer CHECK (proposed_price_ore IS NULL OR proposed_price_ore >= 0),
  preliminary_feedplan jsonb,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::demo_status,
  status_changed_at timestamp with time zone NOT NULL DEFAULT now(),
  sent_at timestamp with time zone,
  opened_at timestamp with time zone,
  responded_at timestamp with time zone,
  resolved_at timestamp with time zone,
  lost_reason text,
  owner_admin_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  converted_customer_id uuid,
  share_token text NOT NULL DEFAULT encode(gen_random_bytes(18), 'hex'::text),
  CONSTRAINT demos_pkey PRIMARY KEY (id),
  CONSTRAINT demos_owner_admin_id_fkey FOREIGN KEY (owner_admin_id) REFERENCES public.team_members(id),
  CONSTRAINT demos_converted_customer_id_fkey FOREIGN KEY (converted_customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.email_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid,
  email_type text NOT NULL,
  subject text NOT NULL,
  recipient_email text NOT NULL,
  content jsonb,
  concepts_included ARRAY,
  status text DEFAULT 'sent'::text,
  error_message text,
  sent_at timestamp with time zone DEFAULT now(),
  scheduled_from uuid,
  CONSTRAINT email_history_pkey PRIMARY KEY (id),
  CONSTRAINT email_history_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT email_history_scheduled_from_fkey FOREIGN KEY (scheduled_from) REFERENCES public.email_schedules(id)
);
CREATE TABLE public.email_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  cm_id uuid NOT NULL,
  recipient_email text NOT NULL,
  subject text NOT NULL,
  body_html text NOT NULL,
  concept_ids ARRAY NOT NULL DEFAULT '{}'::uuid[],
  status text NOT NULL DEFAULT 'queued'::text CHECK (status = ANY (ARRAY['queued'::text, 'processing'::text, 'sent'::text, 'failed'::text, 'canceled'::text])),
  attempts integer NOT NULL DEFAULT 0 CHECK (attempts >= 0),
  max_attempts integer NOT NULL DEFAULT 3 CHECK (max_attempts >= 1 AND max_attempts <= 10),
  last_error text,
  provider_message_id text,
  scheduled_at timestamp with time zone NOT NULL DEFAULT now(),
  locked_at timestamp with time zone,
  locked_by text,
  sent_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT email_jobs_pkey PRIMARY KEY (id),
  CONSTRAINT email_jobs_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT email_jobs_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.email_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  cm_id uuid NOT NULL,
  subject text NOT NULL,
  body_html text NOT NULL,
  concept_ids ARRAY DEFAULT '{}'::uuid[],
  sent_at timestamp with time zone DEFAULT now(),
  CONSTRAINT email_log_pkey PRIMARY KEY (id),
  CONSTRAINT email_log_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT email_log_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.email_schedules (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid,
  schedule_type text NOT NULL DEFAULT 'weekly'::text,
  day_of_week integer,
  send_time time without time zone DEFAULT '09:00:00'::time without time zone,
  rules jsonb DEFAULT '{}'::jsonb,
  email_subject text,
  email_intro text,
  email_outro text,
  is_active boolean DEFAULT true,
  last_sent_at timestamp with time zone,
  next_send_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT email_schedules_pkey PRIMARY KEY (id),
  CONSTRAINT email_schedules_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  type text NOT NULL,
  severity text NOT NULL DEFAULT 'info'::text CHECK (severity = ANY (ARRAY['info'::text, 'warning'::text, 'critical'::text])),
  entity_type text,
  entity_id text,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  read_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT events_pkey PRIMARY KEY (id)
);
CREATE TABLE public.feed_motor_signals (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  signal_type text NOT NULL DEFAULT 'nudge'::text,
  payload jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  acknowledged_at timestamp with time zone,
  auto_resolved_at timestamp with time zone,
  CONSTRAINT feed_motor_signals_pkey PRIMARY KEY (id),
  CONSTRAINT feed_motor_signals_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.feed_spans (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  cm_id uuid NOT NULL,
  frac_start numeric NOT NULL CHECK (frac_start >= 0::numeric AND frac_start <= 1::numeric),
  frac_end numeric NOT NULL,
  climax numeric,
  color_index integer NOT NULL DEFAULT 0 CHECK (color_index >= 0 AND color_index < 10),
  title text NOT NULL DEFAULT ''::text,
  body text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  start_feed_order integer,
  end_feed_order integer,
  CONSTRAINT feed_spans_pkey PRIMARY KEY (id),
  CONSTRAINT feed_spans_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT feed_spans_cm_id_fkey FOREIGN KEY (cm_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.feedplan_concepts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  title text,
  body jsonb,
  created_by_cm_id uuid,
  planned_publish_date date,
  status text NOT NULL DEFAULT 'draft'::text CHECK (status = ANY (ARRAY['draft'::text, 'ready'::text, 'published'::text, 'skipped'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedplan_concepts_pkey PRIMARY KEY (id),
  CONSTRAINT feedplan_concepts_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT feedplan_concepts_created_by_cm_id_fkey FOREIGN KEY (created_by_cm_id) REFERENCES public.team_members(id)
);
CREATE TABLE public.invites (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL UNIQUE,
  token text NOT NULL DEFAULT encode(gen_random_bytes(32), 'hex'::text) UNIQUE,
  business_name text,
  business_description text,
  clip_ids ARRAY DEFAULT '{}'::text[],
  subscription_type text DEFAULT 'growth'::text,
  subscription_price_cents integer,
  subscription_scope text,
  notes text,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  claimed_at timestamp with time zone,
  claimed_by uuid,
  social_links jsonb DEFAULT '{}'::jsonb,
  tone ARRAY DEFAULT '{}'::text[],
  energy text,
  industry text,
  avatar_url text,
  CONSTRAINT invites_pkey PRIMARY KEY (id),
  CONSTRAINT invites_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.profiles(id),
  CONSTRAINT invites_claimed_by_fkey FOREIGN KEY (claimed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.invoice_line_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_line_item_id text NOT NULL UNIQUE,
  stripe_invoice_id text NOT NULL,
  stripe_invoice_item_id text,
  description text,
  amount integer NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'sek'::text,
  quantity integer,
  period_start timestamp with time zone,
  period_end timestamp with time zone,
  environment text CHECK (environment = ANY (ARRAY['test'::text, 'live'::text])),
  data jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT invoice_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_line_items_stripe_invoice_id_fkey FOREIGN KEY (stripe_invoice_id) REFERENCES public.invoices(stripe_invoice_id)
);
CREATE TABLE public.invoices (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_invoice_id text NOT NULL UNIQUE,
  stripe_subscription_id text,
  stripe_customer_id text NOT NULL,
  customer_profile_id uuid,
  user_profile_id uuid,
  amount_due integer NOT NULL DEFAULT 0,
  amount_paid integer NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'sek'::text,
  status text NOT NULL CHECK (status = ANY (ARRAY['draft'::text, 'open'::text, 'paid'::text, 'void'::text, 'uncollectible'::text])),
  hosted_invoice_url text,
  invoice_pdf text,
  due_date timestamp with time zone,
  paid_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  environment text CHECK (environment = ANY (ARRAY['test'::text, 'live'::text])),
  subtotal_ore integer NOT NULL DEFAULT 0,
  tax_ore integer NOT NULL DEFAULT 0,
  total_ore integer NOT NULL DEFAULT 0,
  invoice_number text,
  payment_intent_id text,
  dispute_status text,
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT invoices_user_profile_id_fkey FOREIGN KEY (user_profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.leads (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL,
  source text DEFAULT 'demo'::text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT leads_pkey PRIMARY KEY (id)
);
CREATE TABLE public.pending_invoice_items (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid,
  amount_ore bigint NOT NULL,
  currency text NOT NULL DEFAULT 'sek'::text,
  description text,
  created_at timestamp with time zone DEFAULT now(),
  stripe_invoice_item_id text,
  internal_note text,
  CONSTRAINT pending_invoice_items_pkey PRIMARY KEY (id),
  CONSTRAINT pending_invoice_items_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.pending_stripe_attachments (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid NOT NULL,
  stripe_customer_id text,
  stripe_subscription_id text,
  stripe_product_id text,
  stripe_price_id text,
  reason text NOT NULL DEFAULT 'profile_update_failed'::text CHECK (reason = ANY (ARRAY['profile_update_failed'::text, 'invite_recovery'::text, 'manual_repair'::text])),
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'reconciled'::text, 'failed'::text])),
  retry_count integer NOT NULL DEFAULT 0,
  next_retry_at timestamp with time zone,
  reconciled_at timestamp with time zone,
  last_error text,
  CONSTRAINT pending_stripe_attachments_pkey PRIMARY KEY (id),
  CONSTRAINT pending_stripe_attachments_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  email text NOT NULL,
  business_name text DEFAULT 'Mitt Företag'::text,
  business_description text,
  has_paid boolean DEFAULT false,
  stripe_customer_id text,
  is_admin boolean DEFAULT false,
  subscription_status text,
  subscription_id text,
  subscription_type text,
  subscription_price_cents integer,
  subscription_scope text,
  current_period_end timestamp with time zone,
  has_concepts boolean DEFAULT false,
  social_links jsonb DEFAULT '{}'::jsonb,
  matching_data jsonb DEFAULT '{}'::jsonb,
  tone ARRAY DEFAULT '{}'::text[],
  energy text,
  industry text,
  avatar_url text,
  role USER-DEFINED DEFAULT 'user'::user_role,
  grid_config jsonb DEFAULT '{"rows": 3, "columns": 3, "currentSlotIndex": 4}'::jsonb,
  stepper_inbound_token text UNIQUE,
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.role_changes_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  profile_id uuid NOT NULL,
  old_role USER-DEFINED,
  new_role USER-DEFINED NOT NULL,
  changed_by uuid,
  changed_at timestamp with time zone DEFAULT now(),
  reason text,
  CONSTRAINT role_changes_log_pkey PRIMARY KEY (id),
  CONSTRAINT role_changes_log_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id),
  CONSTRAINT role_changes_log_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.service_costs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  service text NOT NULL,
  date date NOT NULL,
  calls integer NOT NULL DEFAULT 0,
  cost_sek numeric NOT NULL DEFAULT 0,
  metadata jsonb,
  CONSTRAINT service_costs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.service_pricing (
  service text NOT NULL,
  unit text NOT NULL,
  price_ore integer NOT NULL DEFAULT 0,
  source text NOT NULL DEFAULT 'estimate'::text CHECK (source = ANY (ARRAY['measured'::text, 'estimate'::text, 'missing'::text])),
  notes text,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT service_pricing_pkey PRIMARY KEY (service, unit)
);
CREATE TABLE public.service_quotas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  service text NOT NULL UNIQUE,
  used bigint NOT NULL DEFAULT 0,
  limit bigint NOT NULL DEFAULT 0,
  reset_at timestamp with time zone,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT service_quotas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.settings (
  id boolean NOT NULL DEFAULT true CHECK (id = true),
  default_billing_interval text NOT NULL DEFAULT 'month'::text CHECK (default_billing_interval = ANY (ARRAY['month'::text, 'quarter'::text, 'year'::text])),
  default_payment_terms_days integer NOT NULL DEFAULT 14 CHECK (default_payment_terms_days >= 1 AND default_payment_terms_days <= 90),
  default_currency text NOT NULL DEFAULT 'SEK'::text,
  default_commission_rate numeric NOT NULL DEFAULT 0.2000 CHECK (default_commission_rate >= 0::numeric AND default_commission_rate <= 1::numeric),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.stepper_email_imports (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  source text NOT NULL DEFAULT 'gmail'::text CHECK (source = ANY (ARRAY['gmail'::text, 'resend_inbound'::text, 'manual_paste'::text])),
  gmail_message_id text,
  email_subject text,
  email_from text,
  collection_name text,
  creator_username text,
  short_url text NOT NULL,
  resolved_collection_url text,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'importing'::text, 'imported'::text, 'skipped'::text, 'error'::text])),
  error text,
  import_group_id uuid NOT NULL DEFAULT gen_random_uuid(),
  imported_count integer,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stepper_email_imports_pkey PRIMARY KEY (id),
  CONSTRAINT stepper_email_imports_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.stepper_gmail_tokens (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  gmail_address text NOT NULL,
  access_token text NOT NULL,
  refresh_token text NOT NULL,
  token_expiry timestamp with time zone NOT NULL,
  last_polled_at timestamp with time zone,
  last_history_id text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stepper_gmail_tokens_pkey PRIMARY KEY (id),
  CONSTRAINT stepper_gmail_tokens_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.stripe_credit_notes (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_credit_note_id text NOT NULL UNIQUE,
  stripe_invoice_id text,
  stripe_customer_id text,
  customer_profile_id uuid,
  total integer NOT NULL DEFAULT 0,
  refund_amount integer NOT NULL DEFAULT 0,
  credit_amount integer NOT NULL DEFAULT 0,
  out_of_band_amount integer NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'sek'::text,
  reason text,
  memo text,
  status text NOT NULL DEFAULT 'issued'::text,
  effective_at timestamp with time zone,
  raw jsonb,
  environment text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stripe_credit_notes_pkey PRIMARY KEY (id),
  CONSTRAINT stripe_credit_notes_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.stripe_processed_events (
  event_id text NOT NULL,
  event_type text NOT NULL,
  processed_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stripe_processed_events_pkey PRIMARY KEY (event_id)
);
CREATE TABLE public.stripe_refunds (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_refund_id text NOT NULL UNIQUE,
  stripe_charge_id text,
  stripe_payment_intent_id text,
  stripe_invoice_id text,
  stripe_customer_id text,
  customer_profile_id uuid,
  amount integer NOT NULL DEFAULT 0,
  currency text NOT NULL DEFAULT 'sek'::text,
  reason text,
  status text NOT NULL DEFAULT 'pending'::text,
  raw jsonb,
  environment text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stripe_refunds_pkey PRIMARY KEY (id),
  CONSTRAINT stripe_refunds_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.stripe_sync_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_event_id text UNIQUE,
  event_type text NOT NULL,
  object_type text,
  object_id text,
  customer_profile_id uuid,
  source text NOT NULL DEFAULT 'webhook'::text CHECK (source = ANY (ARRAY['webhook'::text, 'manual_resync'::text, 'reconcile_job'::text, 'app_action'::text])),
  status text NOT NULL DEFAULT 'received'::text CHECK (status = ANY (ARRAY['received'::text, 'applied'::text, 'skipped'::text, 'failed'::text])),
  applied_changes jsonb NOT NULL DEFAULT '{}'::jsonb,
  raw_payload jsonb,
  error_message text,
  received_at timestamp with time zone NOT NULL DEFAULT now(),
  processed_at timestamp with time zone,
  environment text,
  CONSTRAINT stripe_sync_events_pkey PRIMARY KEY (id),
  CONSTRAINT stripe_sync_events_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.stripe_sync_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  event_type text NOT NULL,
  stripe_event_id text,
  object_type text CHECK (object_type = ANY (ARRAY['customer'::text, 'subscription'::text, 'invoice'::text, 'invoice_item'::text, 'credit_note'::text, 'charge'::text, 'payment_method'::text, 'other'::text])),
  object_id text,
  sync_direction text CHECK (sync_direction = ANY (ARRAY['stripe_to_supabase'::text, 'supabase_to_stripe'::text])),
  status text NOT NULL CHECK (status = ANY (ARRAY['success'::text, 'failed'::text, 'pending'::text])),
  error_message text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  event_id text,
  environment text NOT NULL DEFAULT 'test'::text,
  payload_summary jsonb,
  CONSTRAINT stripe_sync_log_pkey PRIMARY KEY (id)
);
CREATE TABLE public.subscriptions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  stripe_subscription_id text NOT NULL UNIQUE,
  stripe_customer_id text NOT NULL,
  customer_profile_id uuid,
  user_profile_id uuid,
  status text NOT NULL CHECK (status = ANY (ARRAY['incomplete'::text, 'incomplete_expired'::text, 'trialing'::text, 'active'::text, 'past_due'::text, 'paused'::text, 'canceled'::text, 'unpaid'::text])),
  cancel_at_period_end boolean DEFAULT false,
  currency text NOT NULL DEFAULT 'sek'::text,
  amount integer NOT NULL DEFAULT 0,
  interval text CHECK ("interval" = ANY (ARRAY['day'::text, 'week'::text, 'month'::text, 'year'::text])),
  interval_count integer DEFAULT 1,
  current_period_start timestamp with time zone,
  current_period_end timestamp with time zone,
  trial_start timestamp with time zone,
  trial_end timestamp with time zone,
  canceled_at timestamp with time zone,
  cancel_at timestamp with time zone,
  ended_at timestamp with time zone,
  created timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  environment text CHECK (environment = ANY (ARRAY['test'::text, 'live'::text])),
  pause_until date,
  scheduled_price_change jsonb,
  CONSTRAINT subscriptions_pkey PRIMARY KEY (id),
  CONSTRAINT subscriptions_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id),
  CONSTRAINT subscriptions_user_profile_id_fkey FOREIGN KEY (user_profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.sync_runs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  mode text NOT NULL CHECK (mode = ANY (ARRAY['cron'::text, 'manual'::text, 'mark_produced'::text])),
  started_at timestamp with time zone NOT NULL DEFAULT now(),
  finished_at timestamp with time zone,
  status text NOT NULL DEFAULT 'running'::text CHECK (status = ANY (ARRAY['running'::text, 'ok'::text, 'error'::text])),
  fetched_count integer DEFAULT 0,
  imported_count integer DEFAULT 0,
  stats_updated_count integer DEFAULT 0,
  reconciled boolean DEFAULT false,
  error text,
  calls_used integer DEFAULT 0,
  CONSTRAINT sync_runs_pkey PRIMARY KEY (id),
  CONSTRAINT sync_runs_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.team_customer_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_member_id uuid,
  customer_profile_id uuid,
  assigned_at timestamp with time zone NOT NULL DEFAULT now(),
  unassigned_at timestamp with time zone,
  notes text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT team_customer_history_pkey PRIMARY KEY (id),
  CONSTRAINT team_customer_history_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.team_members(id),
  CONSTRAINT team_customer_history_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.team_members (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  profile_id uuid,
  name text NOT NULL,
  email text NOT NULL UNIQUE,
  phone text,
  role text NOT NULL DEFAULT 'content_manager'::text CHECK (role = ANY (ARRAY['admin'::text, 'content_manager'::text, 'designer'::text, 'other'::text])),
  color text NOT NULL DEFAULT '#4f46e5'::text,
  avatar_url text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  responsibility_area text,
  start_date date,
  invited_at timestamp with time zone,
  bio text,
  region text,
  expertise ARRAY,
  notes text,
  commission_rate numeric NOT NULL DEFAULT 0.2000 CHECK (commission_rate >= 0::numeric AND commission_rate <= 1::numeric),
  city text,
  CONSTRAINT team_members_pkey PRIMARY KEY (id),
  CONSTRAINT team_members_profile_id_fkey FOREIGN KEY (profile_id) REFERENCES public.profiles(id)
);
CREATE TABLE public.tiktok_publications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  tiktok_video_id text NOT NULL,
  published_at timestamp with time zone NOT NULL,
  fetched_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tiktok_publications_pkey PRIMARY KEY (id),
  CONSTRAINT tiktok_publications_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.tiktok_stats (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid NOT NULL,
  snapshot_date date NOT NULL,
  followers integer NOT NULL DEFAULT 0,
  total_videos integer NOT NULL DEFAULT 0,
  videos_last_24h integer NOT NULL DEFAULT 0,
  total_views_24h bigint NOT NULL DEFAULT 0,
  engagement_rate numeric NOT NULL DEFAULT 0,
  raw_payload jsonb,
  fetched_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tiktok_stats_pkey PRIMARY KEY (id),
  CONSTRAINT tiktok_stats_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.tiktok_videos (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_profile_id uuid NOT NULL,
  video_id text NOT NULL,
  uploaded_at timestamp with time zone NOT NULL,
  views bigint NOT NULL DEFAULT 0,
  likes integer NOT NULL DEFAULT 0,
  comments integer NOT NULL DEFAULT 0,
  shares integer NOT NULL DEFAULT 0,
  cover_image_url text,
  share_url text,
  raw_payload jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT tiktok_videos_pkey PRIMARY KEY (id),
  CONSTRAINT tiktok_videos_customer_profile_id_fkey FOREIGN KEY (customer_profile_id) REFERENCES public.customer_profiles(id)
);
CREATE TABLE public.user_clips (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  clip_id text NOT NULL,
  assigned_at timestamp with time zone DEFAULT now(),
  assigned_by uuid,
  notes text,
  is_unlocked boolean DEFAULT false,
  unlocked_at timestamp with time zone,
  CONSTRAINT user_clips_pkey PRIMARY KEY (id),
  CONSTRAINT user_clips_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id),
  CONSTRAINT user_clips_assigned_by_fkey FOREIGN KEY (assigned_by) REFERENCES public.profiles(id)
);
CREATE TABLE public.user_roles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  role USER-DEFINED NOT NULL,
  CONSTRAINT user_roles_pkey PRIMARY KEY (id),
  CONSTRAINT user_roles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);