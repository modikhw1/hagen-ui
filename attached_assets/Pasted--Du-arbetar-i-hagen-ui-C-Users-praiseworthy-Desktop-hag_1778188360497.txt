 Du arbetar i hagen-ui:
  C:\Users\praiseworthy\Desktop\hagen-ui

  Målet är Phase 58: fixa trasiga “Förhandsgranska” och “Synca från hagen” för customer history sync.

  Bakgrund:
  Phase 57 audit hittade att CustomerWorkspaceContent anropar:
  - POST /api/studio-v2/customers/:customerId/sync-history
  - POST /api/studio-v2/customers/:customerId/sync-history?preview=true

  Men i api-server finns bara:
  - GET /api/studio-v2/customers/:customerId/sync-history
  - GET /api/studio-v2/customers/:customerId/hagen-clips

  POST-routen saknas, så UI-flödet är trasigt.

  Uppgift:
  1. Synka hagen-ui main.
  2. Rör inte Hagen-repot.
  3. Inspektera befintliga UI-förväntningar i:
     artifacts/letrend/src/components/studio/customer-detail/CustomerWorkspaceContent.tsx
     - handlePreviewSync
     - handleSyncHistory
     - syncPreviewResult rendering
     - syncHistoryResult rendering

  4. Implementera:
     POST /api/studio-v2/customers/:customerId/sync-history

     Beteende:
     - requireAuth + CM_ONLY
     - ensureCustomerAccess
     - läs kundens tiktok_handle
     - om `?preview=true`:
       - hämta möjliga clips från befintlig Hagen-källa/proxy om tillgänglig
       - jämför mot befintliga customer_concepts/tiktok_url
       - skriv inget till DB
       - returnera shape som UI redan förväntar:
         {
           handle,
           wouldImport,
           wouldSkip,
           totalMatched,
           samples: [...]
         }

     - om inte preview:
       - använd samma jämförelse
       - insert nya rows som history_import/imported_history enligt befintligt schema
       - hoppa över redan importerade tiktok_url
       - returnera:
         {
           imported,
           skipped
         }

  5. Om Hagen-källan inte kan ge clips i detta repo:
     - gör inte en låtsasimport
     - returnera tydlig 502/501 JSON
     - uppdatera UI-copy så knapparna inte framstår som fungerande import
     - dokumentera exakt blocker.

  6. Rör inte RapidAPI direct fetch-flödet:
     - /fetch-profile-history ska fortsätta fungera som idag.
     - Detta pass gäller bara “från hagen”-preview/import.

  7. Ta bort eller quarantina död legacy-fil om säker:
     artifacts/letrend/src/lib/tiktok/trigger-initial-sync.ts

     Endast om `rg` visar att den inte importeras aktivt.

  8. Lägg till dokument:
     docs/agent-plans/58-hagen-library-sync-history-post-route.md

  Dokumentet ska innehålla:
  - rotorsak
  - route contract för preview och import
  - vilken datakälla som används
  - DB-skrivningar vid import
  - vad som händer vid saknad Hagen-källa
  - verifiering
  - kvarvarande risker

  Verifiera:
  - pnpm --filter @workspace/api-server run typecheck
  - pnpm --filter @workspace/letrend run typecheck
  - kör relevanta route/helper-tester om befintlig teststruktur gör det rimligt

  Commit/pusha till main.